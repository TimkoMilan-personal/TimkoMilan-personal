package com.warehouse.warehouse.service;

import com.warehouse.warehouse.model.Users;

public interface UserService {
    /**
     * method add new user to the database
     * Not done
     * return List<Product>
     */
    Users createUser(Users users);
}
