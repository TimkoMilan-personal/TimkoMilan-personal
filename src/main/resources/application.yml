spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/warehouse
    username: admin
    password: admin
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database: postgresql

flyway:
  baselineOnMigrate: true
  user: "admin"
  password: "admin"
  url: jdbc:postgresql://localhost:5432/warehouse


server:
  port: 8080

